#include<stdio.h>
#include<string.h>
int main()
{
    int ar1[5] = {0, 'a', 5};
    ar1 | 0 | 97 | 5 |
    char name[10] = {'a','b'};
    int length = strlen(name);
    int length2 = strlen(ar1);
    printf("ar1 = %d, name = %d", length2, length);
    return 0;
}
